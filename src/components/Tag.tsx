import React from "react";

interface TagProps {
  Tag: string;
}

const Tag: React.FC<TagProps> = (props: TagProps) => {
  const { Tag } = props;

  return <>
  <div>
    
  </div>
  </>;
};

export default Tag;
